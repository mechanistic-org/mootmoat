---
const { href = "", ...rest } = Astro.props;
const external = /^https?:/.test(href) && new URL(href).origin !== Astro.site?.origin;
---
<a href={href} {...rest} target={external ? "_blank" : undefined} rel={external ? "noopener noreferrer" : undefined}><slot /></a>
